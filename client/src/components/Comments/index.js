import React, { useState } from "react";

const Comments = () => {
  const [hasVoted, setHasVoted] = useState(false);

  return <div className="bg-charcoal">placeholder text</div>;
};

export default Comments;
